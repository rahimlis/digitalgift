# Environment Variables for PDF Gift App

# Database Configuration
DATABASE_URL=postgresql://username:password@localhost:5432/pdfgift

# Server Configuration
PORT=3000
NODE_ENV=development

# For Railway deployment, Railway will automatically provide:
# - DATABASE_URL (when you add PostgreSQL service)
# - PORT (Railway sets this automatically)
# - NODE_ENV=production (set automatically)

# You only need to set these manually for local development